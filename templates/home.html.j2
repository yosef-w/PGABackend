<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>INDECT API Integration</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f8f9fa; }
    pre { background-color: #e9ecef; padding: 1rem; border-radius: 0.5rem; }
    .card { border: none; box-shadow: 0 0 10px rgba(0,0,0,0.05); }
    .section-title { border-bottom: 2px solid #0d6efd; padding-bottom: 0.3rem; margin-bottom: 1rem; }
  </style>
</head>
<body>
  <div class="container py-5">
    <div class="card p-4">
      <h1 class="mb-4 text-primary">INDECT API Integration</h1>
      <p class="lead">
        This API is designed specifically for <strong>INDECT</strong> systems to submit and retrieve parking data.
        Use the endpoints below to integrate your INDECT setup with our platform.
      </p>

      <h3 class="section-title">🔗 Base URL</h3>
      <pre><code>{{ base_url }}</code></pre>

      <h3 class="section-title">📌 Available Endpoints</h3>
      <ul class="list-group mb-4">
        <li class="list-group-item">
          <code>POST /api/indect/occupancy</code> — Submit real-time occupancy data
        </li>
        <li class="list-group-item">
          <code>POST /api/indect/license-plate</code> — Submit license plate capture data
        </li>
        <li class="list-group-item">
          <code>GET /api/availability</code> — Retrieve current parking availability
        </li>
        <li class="list-group-item">
          <code>GET /api/find-car/{plate}</code> — Find vehicle location by license plate
        </li>
      </ul>

      <h3 class="section-title">📥 Example Occupancy Payload</h3>
      <pre><code>{
  "Name": "Garage A",
  "Zones": [
    {
      "Name": "Level 1",
      "Zones": [
        {
          "Name": "Zone A1",
          "Bays": {
            "1": "1;available",
            "2": "2;occupied"
          }
        }
      ]
    }
  ]
}</code></pre>

      <h3 class="section-title">📥 Example License Plate Payload</h3>
      <pre><code>{
  "numberPlate": "ABC123",
  "spotId": "1A-101",
  "garageId": 1,
  "carPhotoData": "base64string"
}</code></pre>

      <div class="mt-4">
        <a class="btn btn-primary btn-lg" href="{{ base_url }}/docs">View Full API Documentation (/docs)</a>
      </div>
    </div>
  </div>
</body>
</html>
